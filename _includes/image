<div>
{% capture imagePath %}{{ include.name }}{% endcapture %}
{% if include.caption %}
    <figure>
        <img src="{{ site.baseurl }}/assets/{{ imagePath }}" {% if include.alt %} alt="{{ include.alt }}" {% endif %} {% if include.width %} width="{{ include.width }}" {% endif %}/>
        <figcaption><center>{{ include.caption }}</center></figcaption>
    </figure>    
{% else %}
    <img src="{{ site.baseurl }}/assets/{{ imagePath }}" {% if include.alt %} alt="{{ include.alt }}" {% endif %} {% if include.width %} width="{{ include.width }}" {% endif %}/>
{% endif %}
</div>
